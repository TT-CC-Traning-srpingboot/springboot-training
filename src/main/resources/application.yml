spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password:
    url: jdbc:mysql://localhost:3306/db_file?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true
security:
  basic:
    enabled: false
management:
  security:
    enabled: false

server:
  context-path: /filemanager